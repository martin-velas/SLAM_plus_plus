This is a script which goes through the current directory
and all the subdirectories and converts all the images in
the .tga format to .png format.

It can be used with the -j switch to do the conversion in
parallel.

Requires imagemagick/mogrify and GNU parallel.
